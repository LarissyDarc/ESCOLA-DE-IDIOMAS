# Changelog

All notable changes made in the workspace during this session.

## Unreleased

- Corrected CSS import in `src/main.jsx` (was `./styles.css`, now `./index.css`).
- Improved accessibility in `src/components/Header.jsx`: added `aria-expanded`, `aria-controls`, `role`, and better labels for the menu button and logo.
- Started the Vite dev server to verify changes (available at http://localhost:5174/ in this environment).

---

If you want, I can continue with more refactors (TypeScript, Tailwind, component cleanup) — tell me which direction to take.
